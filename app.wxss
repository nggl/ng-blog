.word-online {
	white-space: nowrap;
	text-overflow: ellipsis;
	overflow: hidden;
}
.hidden {
	visibility: hidden;
}
.original-button {
	position: static;
	padding: 0;
	line-height: normal;
	display: flex;
	align-items: center;
	justify-content: center;
	border: 0;
}
.view-hover {
	opacity: .3;
	background-color: rgba(210, 210, 210, .3);
}